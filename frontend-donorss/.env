# Backend API base used by the donor frontend. Change to your backend host/IP if needed.
REACT_APP_API_URL=http://localhost:5000
# If testing from another device on your LAN, set to the backend machine's LAN IP, e.g.:
#
